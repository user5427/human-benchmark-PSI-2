This readme file summorizes what we (our team) will do in the lab 2 (03-13 to 04-06 aprox.).

**1 Week (03-13)**
This week we plan what we will do in the following weeks, what the goal of the lab 2 will be.

Requirements from lab 1 will be used for designing in lab 2. If during the work we notice that the requirements are too weak or basic, new requirements will be addded.

UML diagrams will be created for the new requirements. As said previously, if the requirements are too weak, improve them. Main requirements are functional (component relations, tasks) and non functional (service responsivness)

If the current architecture is not good for UML design, we can suggest current architecture improvments.

We have too choose new requirement that would allow us to feel what it is like to change a system that we were not working on previously.

Primary tasks: 
- Use UML 4 + 1 framework to organize the architecture document.
- Implement the planned changes in the system
- Introduce a CI/CD process
  - CI - pull request automation, code review automation, automatically check if the code follows style standarts, etc.
  - CD - continously deploy the product to platforms such as Docker containers and Microsoft Azure.
  ** NOTICE ** the pipeline should be created in gitlab platform and documented.

Using color or some kind of symbol, we can highlight the new classes in the UML. The end result is to show of the new requirement.

Class diagram /= informational model

Class diagram - input output of services
Informational model - what info we will save in the DB, entities that store information, we dont go into the implementation of these entities.

Traceability - validation, how to validate that our system is working gud. Check if we didnt miss any requirements, if all objects have entities with other entities, do all objects have representations in class UML diagram.

Again, coming back to the requirements, choose requirements that add more to the structure of the programm asynchronous opperations. How many complex requirements do we need? If we choose one larger requirement, it will likely be enough. If we choose smaller requirements then we will need more of them. During the work ask the lecturer if we have enough requirements.

UML should not be the end goal. We shouldnt try to use all of the diagrams, if we choose difficult requirements naturally we will have most of the UML diagrams. CI/CD diagram...

Whats the difference between use case diagrams in lab 1 and lab 2. lab 1 focuses on business, lab 2 more on the system. Use case - shows the connection of all seperate parts.

Process seperation - for threads...
Use case seperation - for business...



Make status quo, choose the requirements.

<!-- 
This week we will how we will implement the changes, which ones will be prioritized first and which ones will be done last.
Each team member will choose a role that he is more familiar with for faster work output.
We will also discuss which tools we will use for UML moddeling and how the document will be written. -->

**2 Week (03-20)**

Primary tasks:

Design UML diagrams, start developing changes, start working on latex documentation.

**3 Week (03-27)**

Primary tasks:

Start CI/CD process development.
Start making latex slides to show things doen in lab 2.

Show progress to lab lecturer. Ask for improvments.

**4 Week (04-03)**

Practise for presentation and present the work.