%%%%%
%%%%%  Use LUALATEX, not LATEX.
%%%%%
%%%%
\documentclass[]{VUMIFTemplateClass}

\usepackage{indentfirst}
\usepackage{amsmath, amsthm, amssymb, amsfonts}
\usepackage{mathtools}
\usepackage{physics}
\usepackage{graphicx}
\usepackage{verbatim}
\usepackage[hidelinks]{hyperref}
\usepackage{color,algorithm,algorithmic}
\usepackage[nottoc]{tocbibind}
\usepackage{tocloft}
\setlength{\cftfignumwidth}{4em} % Adjust the width as needed
\setlength{\cfttabnumwidth}{4em} % Adjust the width as needed
\usepackage{makecell}
\usepackage{titlesec}

\usepackage{titlesec}
\newcommand{\sectionbreak}{\clearpage}

\makeatletter
\renewcommand{\fnum@algorithm}{\thealgorithm}
\makeatother
\renewcommand\thealgorithm{\arabic{algorithm} algorithm}

\usepackage{biblatex}
\bibliography{bibliografija}
%% to change the numbering (numeric or alphabetic) of bibliographic sources, make the change in VUMIFTemplateClass.cls, line 139

% Author's MACROS
\newcommand{\EE}{\mathbb{E}\,} % Mean
\newcommand{\ee}{{\mathrm e}}  % nice exponent
\newcommand{\RR}{\mathbb{R}}


\studyprogramme{\{Study programme\}} %Write your study programme (example – Software engineering, Financial and Actuarial Mathematics, etc.)
\worktype{\{Work type\}} % Bachelor's thesis or Master's thesis
\worktitle{Work Title in English}
\secondworktitle{UML 1 + 4, requirement implementation, CI/CD}
\workauthor{a̶͗͆i̷̍͝ ̸͑͛ń̸̂e̷̿̎z̴̿̎ȉ̴nau Team}

%There may be more than one author, in which case each author is written from a new line which is added in Titlepage.tex or LongerTitlePage.tex
%\secondauthor{Name Surname} %If present, otherwise delete

\supervisor{Artūras Jankus, Lekt.}

\begin{document}
\selectlanguage{english}

\onehalfspacing
\input{TitlePage}

%% Acknowledgements Section
\sectionnonumnocontent{Acknowledgements}
The team is thankful to VU Mathemathics and Informatics faculty lecturers for their continous involment in helping students understand software engineering principles. Our team is also thankful for the free virtual machine hosting service by our faculty. 

%%Summary
\sectionnonum{Summary}

This work will analyse the application from the system perspective by using UML 4 + 1 method. Other methods will be implemented too that the reader can find below in table of contents. \\


\textbf{Keywords:} Human Benchmark, Cognitive test, Reaction time, Reflex training.





\singlespacing
\selectlanguage{english}
% list of figures, delete if not needed
\listoffigures 

%list of tables, delete if not needed
\listoftables

%Turinys
\tableofcontents
\onehalfspacing


%Section for symbols
\sectionnonum{List of symbols} %Leave if necessary
This section is for when symbols are used. For example:
\begin{itemize}
    \item $\EE X$ denotes the mean of the random variable $X$.
\end{itemize}


%Section for abbreviations
\sectionnonum{List of abbreviations} %Leave if necessary

\begin{tabular}{rcp{.7\textwidth}}
    {UML} & {} & {Unified Modeling Language}
\end{tabular}

\sectionnonum{Introduction}
For any written work, you must refer to the methodology guidelines of the respective study programme\footnote{The latest methodology requirements for all programmes can be found here, in the respective programme: \url{https://mif.vu.lt/lt3/en/studies/bachelor-studies}}. These contain all the guidelines for citations, structure, length, etc.

\section{CI\textbackslash CD}
This section will discuss continous integration and continous delivery of the application. 
The CI (continous integration) is the first process that is done which is then followed by CD (continous delivery).

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/"Deployment Diagram.drawio.png"}
    \caption{Deployment Diagram}
    \label{fig:deployment-diagram}
\end{figure}

\subsection{CI}
The CI (continious integration) is a process where where the application is built and tested after a developer pushes his commit to 
a branch that has CI action script. 

\subsubsection{CI order}
The CI is done in this order:
\begin{itemize}
    \item Stage 1: build the application (The order in which the application is built does not matter, the frontend or backend build process does not depend on each other).
    If building of frontend or backend fails, the CI is cancelled.
    \item Stage 2: test the application (Tests are done to frontend and backend in random order just like in the build stage).
    IF testing of frontend or backend fails, the CI is cancelled.
\end{itemize}

In case CI fails, a red 'X' appears near the commit message in GitLab.
The developer could be informed by an email or other types of communication that the CI proccess for his commit has failed but this is beyond our 
focus.

\subsubsection{CI commands}
The CI process usually is quite straight forward and in our case requires the use
of the basic \textbf{dotnet} and \textbf{npm} commands. These exact commands are used to build the backend:
\begin{enumerate}
    \item \texttt{dotnet restore}
    \item \texttt{dotnet build --configuration Release}
    \item \texttt{dotnet publish -c Release -o ./publish}
\end{enumerate} 
To build the frontend:
\begin{enumerate}
    \item \texttt{npm install}
    \item \texttt{npm run build}
\end{enumerate}

\subsubsection{CI resource needs}

\textbf{Storage needs}

\textbf{Notice:} all of these backend mesurments were done using dotnet 9.0.100 version. \\
To build the backend around 47 megabytes are needed (this includes the bin and obj folder size while excluding all source files). To build integration tests it takes 28 megabytes. To build unit tests it takes 28 megabytes.
The source files for backend take up around 64 kilobytes. The integration test source files take up around 23 kilobytes and the unit test files take up 5 kilobytes. \ref{tab:storage-needs}

\begin{table}[H]
    \centering
    \begin{tabular}{|c|c|c|}
        \hline
        \textbf{Part of application} & \textbf{Source files} & \textbf{bin and obj} \\
        \hline
        Backend files & 64 KB & 47 MB \\
        \hline
        Integration test files & 23 KB & 28 MB \\
        \hline
        Unit test files & 5 KB & 28 MB \\
        \hline
    \end{tabular}
    \caption{Storage needs for the backend}
    \label{tab:storage-needs}
\end{table}

To build the frontent it takes around 87 megabytes (this includes only the node\_modules folder). 
The source files take around 2.6 megabytes. \ref{tab:frontend-storage-needs}

\begin{table}[H]
    \centering
    \begin{tabular}{|c|c|c|}
        \hline
        \textbf{Part of application} & \textbf{Source files} & \textbf{node\_modules} \\
        \hline
        Frontend files & 2.6 MB & 87 MB \\ 
        \hline
    \end{tabular}
    \caption{Storage needs for the frontend}
    \label{tab:frontend-storage-needs}
\end{table}

\textbf{Memory and CPU needs}
Unfortunately it is quite difficult to measure how much certain building or testing processes require memory or CPU on OS platforms such as Windows.
Because of that, we will be measuring the time it takes to build and test the application on our virtual machine.

We will run the processes 4 times and average the result out.

\begin{table}[H]
    \centering
    \begin{tabular}{|c|c|}
        \hline
        \textbf{Process} & \textbf{Time Taken (seconds)} \\
        \hline
        Build Backend & 40; 37; 19; 20  \\
        \hline
        Build Frontend & 32; 27; 27; 31  \\
        \hline
        Test Backend Integration & 18; 17; 11; 10 \\
        \hline
        Test Backend Unit & 14; 18; 14; 13  \\
        \hline
        Test Frontend & 19; 17; 19; 18 \\
        \hline
    \end{tabular}
    \caption{Time taken for various processes}
    \label{tab:process-time}
\end{table}

The mesurments were done in the order that they are displayed in table (first 4 for mesurments for backend building then 4 mesurments for backend integration testing and etc. ). 
It is clear that there is caching done on the building and testing processes as the second mesurment almost always takes less time.\\

The average times for each process are calculated as follows:

\begin{table}[H]
    \centering
    \begin{tabular}{|c|c|}
        \hline
        \textbf{Process} & \textbf{Average Time (seconds)} \\
        \hline
        Build Backend & \(\frac{40 + 37 + 19 + 20}{4} = 29\) \\
        \hline
        Build Frontend & \(\frac{32 + 27 + 27 + 31}{4} = 29.25\) \\
        \hline
        Test Backend Integration & \(\frac{18 + 17 + 11 + 10}{4} = 14\) \\
        \hline
        Test Backend Unit & \(\frac{14 + 18 + 14 + 13}{4} = 14.75\) \\
        \hline
        Test Frontend & \(\frac{19 + 17 + 19 + 18}{4} = 18.25\) \\
        \hline
    \end{tabular}
    \caption{Average time taken for various processes}
    \label{tab:average-process-time}
\end{table}

In total that is about 110 seconds to complete the CI process or about 2 minutes.

\begin{table}[H]
    \centering
    \begin{tabular}{|c|c|c|}
        \hline
        \textbf{Process} & \textbf{Min Time (seconds)} & \textbf{Max Time (seconds)} \\
        \hline
        Build Backend & 19 & 40 \\
        \hline
        Build Frontend & 27 & 32 \\
        \hline
        Test Backend Integration & 10 & 18 \\
        \hline
        Test Backend Unit & 13 & 18 \\
        \hline
        Test Frontend & 17 & 19 \\
        \hline
    \end{tabular}
    \caption{Min and Max time taken for various processes}
    \label{tab:min-max-process-time}
\end{table}

\begin{table}[H]
    \centering
    \begin{tabular}{|c|c|}
        \hline
        \textbf{Process} & \textbf{Difference (seconds)} \\
        \hline
        Build Backend & 40 - 19 = 21 \\
        \hline
        Build Frontend & 32 - 27 = 5 \\
        \hline
        Test Backend Integration & 18 - 10 = 8 \\
        \hline
        Test Backend Unit & 18 - 13 = 5 \\
        \hline
        Test Frontend & 19 - 17 = 2 \\
        \hline
    \end{tabular}
    \caption{Difference between max and min time taken for various processes}
    \label{tab:diff-process-time}
\end{table}

Here we can easily observe cashing mechanisms and optimizations. We can see if the CI pipeline will be run often, the time 
to build and test the application will reduce in total about 40 seconds.

\subsubsection{CI conclusion}
In conclusion, the CI pipeline is not very time intensive nor memory intensive. 
It requires only the most basic command usage. 
In case a failure occurs, the pipeline is canceled and a red 'X' appears near the commit.

\subsection{CD}
The CD (continous delivery) is a process where the application is 
dockerized using Docker and then deployed to a hosting service (in our case our faculty virtual machine).

\subsubsection{CD order}
The CD is done in this order:
\begin{itemize}
    \item Stage 1: dockerize the application (The order in which the application is dockerized does not matter, the frontend or backend build dockerization does not depend on each other).
    If dockerization of frontend or backend fails, the CD is cancelled.
    \item Stage 2: deploy the application (The application is deployd to VM)
    IF deployment fails, the CD is cancelled.
\end{itemize}

In case CD fails, a red 'X' appears near the commit message in GitLab.
Just like in CI section, the developer could be informed by other means but this is beyond the scope of our document.

\subsubsection{CD commands}
The CD process just like CI is straight forward and in our case requires the use
of the basic \textbf{docker} commands. These exact commands are used to dockerize the backend and frontend:
\begin{enumerate}
    \item \texttt{docker build -t frontend-app .}
    \item \texttt{docker build -t backend-app .}
\end{enumerate}

In both cases the docker service finds the images necessary to run the frontend and backend automatically.

The command \texttt{docker-compose up -d} is used to deploy the application.

\subsubsection{CD resource needs}

\textbf{Storage needs}

The commands \texttt{sudo docker ps -a -s}, \texttt{sudo docker system df} and \texttt{sudo docker system df -v} will be used to retrieve this data.\\

SIZE: This is the amount of data that is used by the writable layer of the container (i.e., the changes made to the container's filesystem).

VIRTUAL SIZE: This is the total size of the container, including the read-only image layers and the writable layer.

\begin{table}[H]
    \centering
    \begin{tabular}{|c|c|c|}
        \hline
        \textbf{Part of application} & \textbf{Size} & \textbf{Virtual size} \\
        \hline
        Backend & 0 B & 242 MB \\
        \hline
        Frontend size & 1.09 KB & 50 MB \\ 
        \hline
    \end{tabular}
    \caption{Storage needs retrieved using \texttt{sudo docker ps -a -s}}
    \label{tab:docker-storage-needs}
\end{table}

In total both backend and frontend containers take up around 300 megabytes. 
The reason why the backend container has 0 B for the first column is because the backend container does not write any data to its file system. 
Rather all of the data is stored on a Microsoft SQL database.

\begin{table}[H]
    \centering
    \begin{tabular}{|c|c|c|c|c|}
        \hline
        \textbf{TYPE} & \textbf{TOTAL} & \textbf{ACTIVE} & \textbf{SIZE} & \textbf{RECLAIMABLE} \\ 
        \hline
        Images & 15 & 2 & 2.467GB & 2.439GB (98\%) \\ 
        \hline
        Containers & 2 & 2 & 1.095kB & 0B (0\%) \\ 
        \hline
        Local Volumes & 1 & 1 & 0B & 0B \\ 
        \hline
        Build Cache & 0 & 0 & 0B & 0B \\ 
        \hline
    \end{tabular}
    \caption{Docker resource usage retrieved using \texttt{sudo docker system df}}
    \label{tab:docker-resource-usage}
\end{table}

In table \ref{tab:docker-resource-usage} we can see that the entire application with .Net and Node images takes around 2.5 gigabytes of storage.


\textbf{Memory and CPU needs}
Just like with CI proceses, we will be mesuring the time it takes to dockerize backend and frontend to roughly determine the CPU needs.

We will run the processes 4 times and average the result out.

\begin{table}[H]
    \centering
    \begin{tabular}{|c|c|}
        \hline
        \textbf{Process} & \textbf{Time Taken (seconds)} \\
        \hline
        Dockerize Backend & 35; 4; 3; 3 \\
        \hline
        Dockerize Frontend & 52; 4; 7; 3 \\
        \hline
        Deploy application & 16; 8; 4; 4 \\
        \hline
    \end{tabular}
    \caption{Time taken for various processes}
    \label{tab:dockerize-process-time}
\end{table}

The mesurments were done in the order that they are displayed in table (first 4 for mesurments for backend dockerization then 4 mesurments for frontend dockerization). 
Clearly, when there are changes to backend or frontend, the process takes way longer to build. And if there are no changes done to backend or frontend, the process quickly finishes which is a great 
optimization if the CD pipeline is used often.


The average times for each process are calculated as follows:

\begin{table}[H]
    \centering
    \begin{tabular}{|c|c|}
        \hline
        \textbf{Process} & \textbf{Average Time (seconds)} \\
        \hline
        Dockerize Backend & \(\frac{35 + 4 + 3 + 3}{4} = 11.25\) \\ 
        \hline
        Dockerize Frontend & \(\frac{52 + 4 + 7 + 3}{4} = 16.5\) \\ 
        \hline
        Deploy Application & \(\frac{16 + 8 + 4 + 4}{4} = 8\) \\
        \hline
    \end{tabular}
    \caption{Average time taken for dockerization processes}
    \label{tab:average-dockerize-process-time}
\end{table}

In this case, the average time table is quite inaccurate due to the way how docker works when there are no changes done to backend or frontend.
Because of that, the min and max time \ref{tab:min-max-dockerize-process-time} table will reflect time needs more accurately.+

\begin{table}[H]
    \centering
    \begin{tabular}{|c|c|c|}
        \hline
        \textbf{Process} & \textbf{Min Time (seconds)} & \textbf{Max Time (seconds)} \\ 
        \hline
        Dockerize Backend & 3 & 35 \\ 
        \hline
        Dockerize Frontend & 3 & 52 \\ 
        \hline
        Deploy Application & 4 & 16 \\
        \hline
    \end{tabular}
    \caption{Min and Max time taken for dockerization processes}
    \label{tab:min-max-dockerize-process-time}
\end{table}

\begin{table}[H]
    \centering
    \begin{tabular}{|c|c|}
        \hline
        \textbf{Process} & \textbf{Difference (seconds)} \\ 
        \hline
        Dockerize Backend & 35 - 3 = 32 \\ 
        \hline
        Dockerize Frontend & 52 - 3 = 49 \\ 
        \hline
        Deploy Application & 16 - 8 = 8 \\
        \hline
    \end{tabular}
    \caption{Difference between max and min time taken for dockerization processes}
    \label{tab:diff-dockerize-process-time}
\end{table}

Here we can easily observe cashing mechanisms and optimizations. We can see if the CD pipeline will be run often, the time 
to build and test the application will reduce in total about 90 seconds.

\subsubsection{CD conclusion}
In conclusion, the CD pipeline is efficient and optimized for frequent use. 
The dockerization process benefits significantly from caching mechanisms, 
reducing the time required for subsequent builds. The storage requirements 
are manageable. The deployment process is straightforward, utilizing basic 
Docker commands to build and deploy the application.



\section{Results and Discusion}


\section{Conclusion}

\printbibliography[title = {References and sources}]

\end{document}
